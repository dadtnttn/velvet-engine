// @edition 3
// "17" — deterministic VS3 game simulation assembled from source fragments.
// Textual imports share one function/state namespace and are embedded by Rust.
import "state.vel"

import "core.vel"

import "rooms.vel"

import "combat.vel"

import "ai.vel"

import "interaction.vel"

import "lifecycle.vel"

import "acceptance.vel"
